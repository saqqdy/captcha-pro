/**
 * Mini-program Captcha Shared Constants
 */

/** 默认验证码容器宽度（rpx） */
export const DEFAULT_WIDTH = 650

/** 默认验证码容器高度（rpx） */
export const DEFAULT_HEIGHT = 380

/** 默认滑块宽度（rpx） */
export const DEFAULT_SLIDER_WIDTH = 80

/** 默认滑块高度（rpx） */
export const DEFAULT_SLIDER_HEIGHT = 80

/** 默认请求超时时间（ms） */
export const DEFAULT_TIMEOUT = 10000

/** 验证失败后自动刷新延迟（ms） */
export const FAIL_REFRESH_DELAY = 800

/** 点击验证码默认字数 */
export const DEFAULT_CLICK_COUNT = 3
