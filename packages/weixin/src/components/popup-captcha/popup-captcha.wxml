<view wx:if="{{visible}}" class="popup-captcha" aria-role="dialog" aria-label="{{title}}">
  <!-- Mask -->
  <view class="popup-mask" catchtap="onMaskTap" aria-label="关闭" aria-role="button" />

  <!-- Content card -->
  <view class="popup-content">
    <!-- Header -->
    <view class="popup-header">
      <text class="popup-title">{{title}}</text>
      <view wx:if="{{showClose}}" class="popup-close" catchtap="hide" aria-label="关闭" aria-role="button">
        <text>×</text>
      </view>
    </view>

    <!-- Captcha body -->
    <view class="popup-body">
      <slider-captcha
        wx:if="{{type === 'slider'}}"
        width="{{sliderOptions.width}}"
        height="{{sliderOptions.height}}"
        sliderWidth="{{sliderOptions.sliderWidth}}"
        sliderHeight="{{sliderOptions.sliderHeight}}"
        showRefresh="{{sliderOptions.showRefresh}}"
        backend="{{backend}}"
        bind:success="onSuccess"
        bind:fail="onFail"
        bind:refresh="onRefresh"
      />
      <click-captcha
        wx:else
        width="{{clickOptions.width}}"
        height="{{clickOptions.height}}"
        showRefresh="{{clickOptions.showRefresh}}"
        backend="{{backend}}"
        bind:success="onSuccess"
        bind:fail="onFail"
        bind:refresh="onRefresh"
      />
    </view>
  </view>
</view>
